# Experiments

Use this directory for lightweight run artifacts that should stay in version control, such as:
- experiment notes
- config snapshots
- metric summaries
- decisions made after each training or evaluation pass

Large checkpoints, logs, and generated artifacts should stay outside git.
